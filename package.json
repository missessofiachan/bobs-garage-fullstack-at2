{
	"name": "bobs-garage-fullstack",
	"version": "1.0.0",
	"description": "A modern full-stack web application for managing an auto repair shop",
	"private": true,
	"license": "ISC",
	"author": "",
	"repository": {
		"type": "git",
		"url": ""
	},
	"keywords": [
		"fullstack",
		"react",
		"typescript",
		"express",
		"mysql",
		"monorepo"
	],
	"packageManager": "yarn@1.22.22",
	"engines": {
		"node": ">=20.0.0",
		"yarn": ">=1.22.19"
	},
	"workspaces": [
		"server",
		"client"
	],
	"scripts": {
		"dev": "concurrently \"yarn workspace server dev\" \"yarn workspace client dev\"",
		"build": "yarn workspace server build && yarn workspace client build",
		"start": "yarn workspace server start",
		"lint": "node_modules/@biomejs/cli-win32-x64/biome.exe lint .",
		"lint:fix": "node_modules/@biomejs/cli-win32-x64/biome.exe check --write 4.",
		"format": "node_modules/@biomejs/cli-win32-x64/biome.exe format --write .",
		"format:check": "node_modules/@biomejs/cli-win32-x64/biome.exe format .",
		"typecheck": "yarn workspace server typecheck && yarn workspace client typecheck",
		"test": "yarn workspace server test && yarn workspace client test",
		"test:watch": "concurrently \"yarn workspace server test:watch\" \"yarn workspace client test:watch\"",
		"clean": "yarn workspace server clean && yarn workspace client clean || true",
		"update:dependencies": "yarn upgrade-interactive --latest"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.3",
		"@commitlint/cli": "^20.1.0",
		"@commitlint/config-conventional": "^20.0.0",
		"@types/node": "^24.10.0",
		"concurrently": "^9.1.2",
		"husky": "^9.1.5",
		"lint-staged": "^16.2.6"
	},
	"lint-staged": {
		"*.{js,jsx,ts,tsx,json,md,css,scss}": [
			"node_modules/@biomejs/cli-win32-x64/biome.exe check --write"
		]
	},
	"dependencies": {
		"dompurify": "^3.3.0",
		"express-validator": "^7.3.0",
		"jsdom": "^27.0.1"
	}
}
